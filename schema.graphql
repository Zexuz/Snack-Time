# This file was generated based on ".graphqlconfig". Do not edit manually.

schema {
    query: RootQuery
    mutation: RootMutation
}

type Episode {
    filepath: String
    number: Int!
    title: String!
}

type Images {
    coverType: Cover!
    url: String!
}

type RootMutation {
    playFile(filepath: String!): Boolean
    updateSettings(mpvPath: String!): Settings
}

type RootQuery {
    "Fetches all series"
    series: [Series!]!
    seriesById(
        "Id of series to fetch"
        SeriesId: Int
    ): Series
    "Get all settings"
    settings: Settings!
}

type Season {
    episodes: [Episode!]!
    number: Int!
}

type Series {
    id: Int!
    images: [Images!]!
    seasons: [Season!]!
    title: String!
}

type Settings {
    mpvPath: String
}

"What kind of cover image"
enum Cover {
    "banner"
    BANNER
    "fanart"
    FANART
    "poster"
    POSTER
}
